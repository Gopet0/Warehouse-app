@model StockTransaction
@{
    ViewBag.Title = "New Transaction";
}

<h1>New Stock Transaction</h1>

<div class="card">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label>Type</label>
            @Html.DropDownListFor(m => m.Type, new SelectList(new[] {
                new { Value = "stock_in", Text = "Stock In" },
                new { Value = "stock_out", Text = "Stock Out" }
            }, "Value", "Text"), "-- Select Type --")
        </div>
        <div class="form-group">
            <label>Product</label>
            @Html.DropDownList("ProductId", (SelectList)ViewBag.ProductId!, "-- Select Product --")
        </div>
        <div class="form-group">
            <label>Quantity</label>
            @Html.TextBoxFor(m => m.Quantity, new { type = "number", min = "1" })
        </div>
        <div class="form-group">
            <label>Supplier (for Stock In)</label>
            @Html.DropDownList("SupplierId", (SelectList)ViewBag.SupplierId!, "-- None --")
        </div>
        <div class="form-group">
            <label>Notes</label>
            @Html.TextAreaFor(m => m.Notes, 3, 0, null)
        </div>

        <button type="submit" class="btn btn-primary">Record Transaction</button>
        <a href="/StockTransaction" class="btn btn-danger" style="margin-left:10px;">Cancel</a>
    }
</div>
